apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: auth-dev
  namespace: kargobook-demo
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: auth-warehouse
      sources:
        direct: true
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/genekuo-org/demo-gitops.git
            checkout:
              - branch: main
                path: ./repo
        - uses: kustomize-set-image
          config:
            path: ./repo/apps/auth/envs/dev
            images:
              - image: ghcr.io/genekuo-org/auth-service
        - uses: git-commit
          config:
            path: ./repo
            message: "promote auth to dev: ${{ freight.image.tag }}"
        - uses: git-push
          config:
            path: ./repo
            targetBranch: main
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: auth-pre-prod
  namespace: kargobook-demo
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: auth-warehouse
      sources:
        stages:
          - auth-dev
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/genekuo-org/demo-gitops.git
            checkout:
              - branch: main
                path: ./repo
        - uses: kustomize-set-image
          config:
            path: ./repo/apps/auth/envs/pre-prod
            images:
              - image: ghcr.io/genekuo-org/auth-service
        - uses: git-commit
          config:
            path: ./repo
            message: "promote auth to pre-prod: ${{ freight.image.tag }}"
        - uses: git-push
          config:
            path: ./repo
            targetBranch: main
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: auth-prod
  namespace: kargobook-demo
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: auth-warehouse
      sources:
        stages:
          - auth-pre-prod
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/genekuo-org/demo-gitops.git
            checkout:
              - branch: main
                path: ./repo
        - uses: kustomize-set-image
          config:
            path: ./repo/apps/auth/envs/prod
            images:
              - image: ghcr.io/genekuo-org/auth-service
        - uses: git-commit
          config:
            path: ./repo
            message: "promote auth to prod: ${{ freight.image.tag }}"
        - uses: git-push
          config:
            path: ./repo
            targetBranch: main
